#include <stdlib.h>
#include <stdarg.h>
#include "main.h"

/**
 * _printf - function that returns number of characters printed
 * @format: character string
 * Return: integer
 */

int _printf(const char *format, ...)
{
int i;

va_list character[];
va_start(character[], format);

for (i = 0 ; character[i] < '\0' ; i++ )
int num = va_arg(character[i], int);
return (num);

}
